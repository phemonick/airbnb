import { StyleSheet } from 'react-native';

export const popStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#00b7ad',
    padding: 20,
    color: '#fff',
  },
  textColor: {
    color: '#fff'
  }
});
